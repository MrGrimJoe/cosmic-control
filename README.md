

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key or whichever key you want since the engine is a bit too complex to explain to everyone.
3. Run the app:
   `npm run dev`
