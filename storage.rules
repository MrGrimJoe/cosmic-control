rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /orgs/{orgId}/{allPaths=**} {
      allow read: if request.auth != null; // Anyone authenticated can read files (for now)
      allow write: if request.auth != null; // Anyone authenticated can upload
    }
  }
}
